<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WindowWeather" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Background.Theme.WindowWeather" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/WhiteSmoke</item><!--浅灰白,title栏背景色-->
        <item name="colorPrimaryDark">@color/black</item>
    </style>

    <style name="Theme.WindowWeather" parent="Base.Theme.WindowWeather" />

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light">
        <item name="actionMenuTextColor">@color/white</item> <!--颜色-->
        <item name="android:textSize">10sp</item> <!--字体大小-->
    </style>

    <style name="noAnimationTheme" parent="Theme.AppCompat.Light.NoActionBar">这里也要注意  如果你的没有Apptheme会报空指针的！！！！！！！！！！！
        <item name="android:windowAnimationStyle">@style/noAnimation</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="noAnimation">
        <item name="android:activityOpenEnterAnimation">@null</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@null</item>
        <item name="android:taskOpenEnterAnimation">@null</item>
        <item name="android:taskOpenExitAnimation">@null</item>
        <item name="android:taskCloseEnterAnimation">@null</item>
        <item name="android:taskCloseExitAnimation">@null</item>
        <item name="android:taskToFrontEnterAnimation">@null</item>
        <item name="android:taskToFrontExitAnimation">@null</item>
        <item name="android:taskToBackEnterAnimation">@null</item>
        <item name="android:taskToBackExitAnimation">@null</item>

    </style>
</resources>